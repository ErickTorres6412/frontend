import { Package } from 'lucide-react';
  
  const modules = [
    {
      title: 'CRUD',
      icon: Package,
      subItems: [
        { label: 'Usuarios', link: '/usuarios' }
      ]
    }
  ];
  
  export default modules;