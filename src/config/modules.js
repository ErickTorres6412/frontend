import { Package } from 'lucide-react';
  
  const modules = [
    {
      title: 'CRUD',
      icon: Package,
      subItems: [
        { label: 'Empleados', link: '/empleados' }
      ]
    }
  ];
  
  export default modules;